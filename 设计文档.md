# PN532 Flutter 读写器应用 - 界面设计规范文档

## 1\. 设计概述 (Design Overview)

  * **目标设备**: PN532 NFC/RFID 读写模块 (通过 USB/TTL 串口连接)。
  * **开发框架**: Flutter (支持 Desktop: Windows/macOS/Linux & Mobile)。
  * **视觉风格**: 极简主义、圆润、柔和色调 (借鉴 Chameleon Ultra GUI)。
  * **核心布局**: 左侧固定导航栏 + 右侧动态内容区域 (Split View)。

## 2\. 视觉规范 (Visual Identity)

### 2.1 配色方案 (Color Palette)

基于参考图片提取的近似色值：

  * **主背景色 (Main Background)**: `#FFFBFB` (极淡的粉白，营造温暖感)
  * **侧边栏背景 (Sidebar)**: `#F5F5F5` 或透明度极高的白色，与主内容区要有微妙的分隔。
  * **主色调/选中状态 (Primary/Active)**: `#FFE0D6` (淡鲑鱼粉) 或 `#FFCcc7`。
  * **强调图标色 (Icon Color)**: `#5A4D4D` (深褐灰色，比纯黑柔和)。
  * **非活性/禁用 (Disabled)**: `#D1C4C4` (浅灰)。
  * **文字颜色 (Text)**: `#2C2C2C` (主标题), `#666666` (副标题)。

### 2.2 字体与图标 (Typography & Icons)

  * **字体**: 使用无衬线字体 (San Francisco, Roboto, 或 Noto Sans SC)。
  * **图标**: 推荐使用 `CupertinoIcons` (iOS 风格) 或 `Material Community Icons`，线条需圆润。

-----

## 3\. 界面布局结构 (Layout Structure)

应用采用 **Master-Detail (左右分栏)** 布局：

### 3.1 左侧：导航栏 (Sidebar Navigation)

  * **宽度**: 固定宽度 (约 200px - 250px)。
  * **Logo/标题**: 顶部显示 "PN532 Tools" 或你的应用名称。
  * **菜单项 (Menu Items)**: 垂直排列，选中项具有圆角矩形背景（如图片中的 Home 所示）。
      * **连接 (Home/Connect)**: 串口配置与连接状态。
      * **读卡 (Read Tag)**: 读取卡片数据。
      * **写卡 (Write Tag)**: 写入数据到卡片。
      * **卡包 (Saved Dumps)**: 管理已保存的 `.mfc` / `.bin` 文件。
      * **工具 (Tools)**: 唤醒 PN532、高级 APDU 命令控制台。
      * **设置 (Settings)**: 串口波特率设置、主题切换。

### 3.2 右侧：内容区域 (Content Area)

  * **标题栏**: 显示当前功能名称（如 "Connect", "Read Card"）。
  * **操作区**: 右上角放置全局操作按钮（如“刷新”、“断开连接”）。
  * **主体**: 根据功能变化的动态面板。

-----

## 4\. 详细页面功能描述 (Page Descriptions)

### 4.1 首页 / 连接页 (Home - Connect)

*参考图片中的当前页面*

  * **功能**: 扫描并连接 PN532 硬件。
  * **UI 元素**:
      * **大标题**: "Connect Device"。
      * **设备列表**: 中央显示检测到的串口设备列表 (如 `COM3`, `/dev/ttyUSB0`)。
          * *样式*: 卡片式列表，显示设备名称和路径。
      * **刷新按钮**: 右上角（如图片中的圆圈箭头），用于重新扫描串口。
      * **浮动按钮 (+)**: 右下角，用于手动输入串口地址（如果自动扫描失败）。
      * **状态指示**: 底部或顶部显示 "PN532 Online/Offline" 状态灯。

### 4.2 读卡页 (Read Card)

  * **功能**: 读取 NFC 卡片（Mifare Classic 1K/4K 等）。
  * **UI 元素**:
      * **读取模式选择**: 单选按钮组 (Read Full / Read Sector / Read Block)。
      * **密钥输入**: 两个输入框 "Key A" 和 "Key B" (默认 `FFFFFFFFFFFF`)。
      * **操作按钮**: 一个大的 "Start Reading" 按钮。
      * **数据展示区**:
          * **Hex View**: 经典的十六进制编辑器视图，按 Sector 0-15 分组显示。
          * **颜色编码**: UID (0扇区0块) 用红色高亮，Key A/B 用蓝色/绿色高亮。

### 4.3 写卡页 (Write Card)

  * **功能**: 将数据写入白卡或 UID 卡。
  * **UI 元素**:
      * **数据源选择**:
          * 选项 1: "Load from File" (从卡包选择)。
          * 选项 2: "Clone from Reader" (从当前读取的缓存)。
      * **写入选项**: Checkbox "Write Manufacturer Block (Block 0)" (警告：仅适用于 UID/CUID 卡)。
      * **进度条**: 写入时的圆形或线性进度条。

### 4.4 卡包 / 文件管理 (Saved Cards)

  * **功能**: 管理 `.dump`, `.mfc`, `.bin` 文件。
  * **UI 元素**:
      * **网格/列表视图**: 显示已保存的文件，图标显示卡片类型。
      * **右键菜单**: 重命名、删除、导出、Hex 编辑。

-----

## 5\. Flutter 实现建议 (Technical Tips)

为了还原图中的效果，建议使用以下 Flutter 组件和库：

1.  **导航栏实现**:
    不要直接使用标准的 `NavigationRail` (因为它通常太窄)。建议使用 `Row` 布局：

    ```dart
    Row(
      children: [
        // Sidebar
        Container(
          width: 250,
          color: Color(0xFFFBFBFB), // 侧边栏背景
          child: Column(
            children: [
              // Logo Area
              _buildHeader(), 
              // Menu Items (自定义 ListTile)
              _buildMenuItem(icon: Icons.home, title: "Home", isActive: true),
              _buildMenuItem(icon: Icons.nfc, title: "Read Card"),
              // ...
            ],
          ),
        ),
        // Main Content
        Expanded(
          child: Container(
            color: Color(0xFFFFFBFB), // 主背景
            child: _buildPageContent(),
          ),
        ),
      ],
    )
    ```

2.  **串口通信**:
    使用 `flutter_libserialport` 库来处理 PN532 的数据传输。

3.  **Hex 视图**:
    不要自己手画所有 Grid。可以使用 `google_fonts` 中的等宽字体 (如 Roboto Mono) 配合 `ListView.builder` 来渲染十六进制数据块。

4.  **状态管理**:
    建议使用 `Provider` or `Riverpod`。因为 PN532 的连接状态（Connected/Disconnected）是全局的，需要影响导航栏中某些按钮的可用性（例如：未连接时，Read/Write 按钮应置灰）。

### 5.1 侧边栏按钮样式代码示例

为了达到图片中选中项“粉色背景 + 黑色文字”的效果：

```dart
Widget _buildMenuItem({
  required IconData icon, 
  required String title, 
  bool isActive = false
}) {
  return Container(
    margin: EdgeInsets.symmetric(horizontal: 16, vertical: 4),
    decoration: isActive 
      ? BoxDecoration(
          color: Color(0xFFFFE0D6), // 选中的粉色背景
          borderRadius: BorderRadius.circular(12),
        )
      : null,
    child: ListTile(
      leading: Icon(
        icon, 
        color: isActive ? Color(0xFF333333) : Color(0xFF9E9E9E)
      ),
      title: Text(
        title,
        style: TextStyle(
          color: isActive ? Color(0xFF333333) : Color(0xFF9E9E9E),
          fontWeight: isActive ? FontWeight.w600 : FontWeight.normal,
        ),
      ),
      shape: RoundedRectangleBorder(borderRadius: BorderRadius.circular(12)),
    ),
  );
}
```